# Runs with zero configuration. To override anything, copy .env.example
# to .env, edit it, and uncomment env_file below.
services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    # env_file:
    #   - .env
    restart: unless-stopped

fastapi==0.115.0
uvicorn[standard]==0.30.6
psutil==7.0.0
sqlalchemy==2.0.52
pydantic-settings==2.5.2
requests==2.32.3
websockets==13.1
pytest==8.3.3
httpx==0.27.2
speedtest-cli==2.1.3

# Windows-only: enables real per-process MB tracking via ETW (see
# backend/collectors/traffic_collector_windows.py). Harmless no-op
# install-wise on Linux/macOS (marker skips it); the app falls back to
# the connection-count proxy there regardless.
pywintrace==0.2.0; sys_platform == "win32"
